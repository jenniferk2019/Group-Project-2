DROP DATABASE IF EXISTS thirdwheel_db;
CREATE DATABASE thirdwheel_db;

-- DROP DATABASE IF EXISTS thirdwheel_db;

-- CREATE DATABASE userData (
-- userid INT AUTO_INCREMENT NOT NULL,
-- userHandle VARCHAR (20) NOT NULL,
-- userPassword VARCHAR (12) NOT NULL,
-- userBio VARCHAR (250) NOT NULL,
-- PRIMARY KEY (userid)
-- );

-- INSERT INTO userData (userHandle, userPassword, userBio)
-- VALUES ("jsmith", "123abc", "Born in a class-5 hurricane");

-- CREATE DATABASE hangouts(
--     hangoutId INT AUTO_INCREMENT NOT NULL,
--     hangout_name VARCHAR (20) NOT NULL,
--     FOREIGN KEY userhandle,
--     hangout_location 
-- )
